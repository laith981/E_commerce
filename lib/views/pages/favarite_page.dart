import 'package:flutter/material.dart';

class FavaritePage extends StatelessWidget {
  const FavaritePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("FavaritePage"),
    );
  }
}